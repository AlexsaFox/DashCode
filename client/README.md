# Frontend part of DashCode

## Prepare environment
On Windows:
 - Install `Node.js` for `npm` package manager.
 - Run:
 ```bash
    npm i -g pnpm
    pnpm i
    pnpm dev
 ```
 And your are ready to go!
